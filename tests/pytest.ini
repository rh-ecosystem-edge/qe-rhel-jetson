[pytest]
addopts = 
    -s
    --strict-markers
    -W always::UserWarning
    --log-cli-level=INFO
markers =
    critical: Critical tests that must pass
    xfail: Tests that are expected to fail
filterwarnings =
    always::UserWarning
